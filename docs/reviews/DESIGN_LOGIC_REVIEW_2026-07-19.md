# Аудит дизайна и логики экранов сессии — 2026-07-19

Заказ владелицы: «глобальная проверка стиля по дизайну и логике внешнего вида —
чтобы КАЖДОЕ дыхание было логично по алгоритму, отображению надписей, пропорциям
положения надписей и по самой логике». Это ОТЧЁТ (находки не чинились).

Проверено: `lib/domain/catalog/techniques.dart` (23 техники), рендер фигур
`lib/features/session/breathing_painter.dart`, экраны
`session/session_view.dart`, `session/session_runner.dart`,
`timer_session/timer_session_screen.dart`, `wim_hof/wim_hof_session_screen.dart`,
подписи `session/phase_labels.dart`, `session/segment_labels.dart`, тексты
`l10n/app_ru.arb` / `app_en.arb`. Плюс golden-скриншоты `SessionView` во всех
формах/фазах, на ru/en, с textScale (см. секцию «Скриншоты»).

---

## Сводка — главные проблемы

1. **P1. Таймер-техники (6 шт.) — практически пустой экран практики.** У
   `TimerSessionScreen` во время практики нет НИКАКИХ подписей фаз («вдох/выдох»)
   и нет пояснения, что делать; виден только крупный обратный таймер и (для Нади
   Шодханы) подсказка ноздри. При выключенных подсказках (Нади «свободный режим»,
   и всегда у orbit/mindful/axis/diaphragmatic/sound_breath) на экране — только
   цифры таймера. Это прямая жалоба владелицы, и она распространяется на всю
   группу timer.

2. **P1. Подпись ноздри Нади Шодханы «гуляет» вместе с дышащим кругом.**
   Подпись «Левая/Правая ноздря» лежит в том же `Column`, что и пульсирующий круг
   (размер круга анимируется `glow`), — прыгает по вертикали с каждым дыханием.
   Нужна статичная раскладка (см. отдельную секцию «Нади Шодхана»).

3. **P1/P2. Описания техник обещают то, чего на экране НЕТ.** `sound_breath`,
   `diaphragmatic`, `nadi_shodhana` в описании обещают «голосовую инструкцию в
   начале» — её в `TimerSessionScreen` не существует. `sound_breath` обещает
   «опциональные подсказки „вдох / гудите на выдохе“» — в UI их нет вовсе.
   «Девять дыханий»: карточка и метка сегмента говорят «вдох левой / выдох
   правой», а подпись фазы под кругом одновременно показывает «Вдох носом /
   Выдох носом» — два противоречащих указания на одном экране.

4. **P2. Жёсткие цвета в экранах сессии** (нарушение принципа №2 владелицы «ни
   одного `Color(0xFF` в экранах»): `segment_labels.dart` `elementColor()` —
   9 захардкоженных `Color(0xFF…)` (стихии и тройки девяти дыханий). Вторая тема
   их не подхватит.

5. **P2. Английские и длинные (textScale) подписи режутся в центре круга.**
   Подпись фазы/маршрута рисуется В ЦЕНТРЕ круга шириной 200 px с `maxLines: 2`.
   Длинные строки — «Exhale through the mouth», «Silent, subtle breath», а также
   любые при крупном системном шрифте — упираются в ellipsis / налезают на
   отсчёт. (Детали — по скриншотам.)

---

## Таблица по техникам

| Техника (id) | Тип / форма / экран | Находки |
|---|---|---|
| Квадрат (box) | counted / square / SessionView | 4 фазы = 4 стороны — точка обходит квадрат логично. `phaseCount` захардкожен =4 в пейнтере (ок: единственная square-техника). |
| Треугольник (triangle) | counted / triangle / SessionView | 3 фазы (вдох/задержка/выдох). Геометрия основанием вверх: вдох-подъём, задержка-горизонталь, выдох-спуск — логично (влад. 2026-07-19). ОК. |
| 4-7-8 (four_seven_eight) | counted / circle | Круг: вдох-рост, задержка-полный, выдох-спад. Логично. Задержки нет holdOut — ок. |
| 4-2-4 (four_two_four) | counted / circle | ОК; солнышко (energizing). |
| 2-8 / 2-10 | counted / circle | Только вдох+выдох, удлинённый выдох виден как долгий спад. ОК. |
| Физиологический вздох (sigh) | counted / circle | Двойной вдох — только текстом («доберите на пике»); фигура одна волна. Приемлемо по замыслу, но на экране нет намёка на «довдох» — см. P3. |
| 4-16-8 | counted / circle | Задержка 16 c: круг статичен-полный 16 c, отсчёт идёт. ОК логически. |
| Когерентное (coherent) | counted / circle | ОК (5.5/5.5). |
| Вытягивающее (stretch) | scripted / circle | Растущий выдох виден как всё более долгий спад; отсчёт секунд показывает до 28. Нет индикации «дыхание N из 25» кроме «Цикл N/25». Приемлемо. |
| По элементам (elemental) | scripted / circle + сегменты | Метка стихии + цвет + маршрут. Цвета захардкожены (P2). EN-маршруты длинные — обрезаются в центре круга (P2). |
| Девять дыханий (nine_breaths) | scripted / circle + сегменты | Метка «Вдох левой · выдох правой» ПРОТИВОРЕЧИТ подписи фазы «Вдох носом» (обе на экране). Маршрут в данных nose/nose — не отражает лево/право (P1/P2). |
| Фикр (fikr) | counted / circle + фразы | Фраза над кругом, `maxWidth 300`, `maxLines 2`, ellipsis. Длинные кастомные фразы обрезаются (P2). |
| Сосуд (vessel) | counted / circle | вдох/задержка/выдох на круге — ОК. |
| Бандхи (bandha) | counted / circle | ОК. |
| Малая орбита (orbit) | timer / круг-свечение | Пустой экран практики (P1). |
| Энерговолна (energy_wave) | counted / circle | ОК (6-6). |
| Диафрагмальное (diaphragmatic) | timer | Пустой экран; описание обещает голос-интро (P1/P2). |
| Осознанное (mindful) | timer | Пустой экран практики (P1). |
| Нади Шодхана (nadi_shodhana) | timer + cue | Подпись ноздри гуляет; нет фаз; «свободный режим» = пустой экран (P1). |
| По оси (axis) | timer | Пустой экран практики (P1). |
| Со звуком (sound_breath) | timer | Пустой экран; описание обещает голос-интро и подсказки «вдох/гудите» — их нет (P1/P2). |
| Вим Хоф (wim_hof) | wimHof / свой экран | Дыхание: подпись «Дышите…» под кругом (ок). Задержка/восстановление/финиш — см. детальные находки. |

---

## Детальные находки

_(заполняется; скриншоты — ниже)_
